Question 2: Last Digit of a^b

Difficulty: MediumAccuracy: 23.8%Submissions: 110K+Points: 4
Given two integers a and b in the form of strings. Return the last digit of ab.

Examples:
Input: a = "3", b = "10"
Output: 9
Explanation: 310 = 59049. Last digit is 9.

Input: a = "6", b = "2"
Output: 6
Explanation: 62 = 36. Last digit is 6.

Constraints:
1 ≤ a.size(), b.size() ≤ 1000
a and b consist only of numeric digits ('0' - '9')
a and b do not contain any leading zeros, except when number itself is "0"

SOLUTION:

class Solution {
    public int getLastDigit(String a, String b) {
        // code here
       if(b.equals("0")){
           return 1;
       }
       int last=a.charAt(a.length()-1)-'0';
       int exp=0;
       for(int i=0;i<b.length();i++){
       exp=(exp*10 + (b.charAt(i)-'0'))%4;
       }
       if(exp==0){
           exp=4;
       }
       int ans=1;
       for(int i=0;i<exp;i++){
           ans=(ans*last)%10;
       }
       return ans;
    }
};
